# 결정 기록

새 결정은 번호를 이어 근거와 함께 추가한다.
결정 4~17은 사전 등록 절차(결과 확인 전 기록, 변경 시 deviation 기록)로 만들었다. **이 절차는 결정 18(2026-09-15)부터 폐기했다.** 기존 기록과 공식 결과는 그대로 둔다.

| # | 결정 | 근거 |
|---|---|---|
| 1 | **제품은 "서울 법정동별 집값 상승률 예측 + 소속 단지 사실정보 조회"다. "투자가치"는 이름·소개에만 쓰고, 화면에는 사실과 예측 수치만 쓴다** (2026-09-15) | 팀 회의 결과 방향을 바꿨다. 임앤장의 판단 표현 금지 규칙(저평가/고평가/유망/추천 등)은 유지한다. 예측 수치에는 "추정"과 구간을 붙인다 |
| 2 | **실거래는 매매 2006-01, 전월세 2011-01부터 쓴다** (2026-09-15) | 임앤장 수집분 60개월(2021-09~2026-08)은 시장 사이클이 하나뿐이고, 1년 예측의 평가 기점이 약 36개월로 제한된다. API 실측 결과 강남구 기준 전월세는 2006-01에 0건, 2011-01부터 존재했다. 과거 매매도 `aptSeq`가 100% 채워져 있다 |
| 3 | **스크립트 번호는 40번부터 쓴다** | 임앤장 산출물(`output/01.*`~`37.*`)을 그대로 복사해 쓰므로 같은 번호를 쓰면 산출물 이름이 겹친다. 수집 스크립트 `11`은 임앤장 산출물 이름을 유지하려고 번호를 그대로 둔다 |

## 사전 등록 — 동별 상승률 예측 (2026-09-15, 결과 확인 전)

| # | 결정 | 근거 |
|---|---|---|
| 4 | **동 키 = `sggCd` + `umdNm`** (자치구 코드 + 법정동명) | 임앤장 결정 56: 법정동 코드 5자리만 쓰면 여러 구가 섞여 효과가 과대 측정됐다 |
| 5 | **target 지수 = hedonic.** 분기 단위. 표본은 취소 제외, 가격·면적 양수, `aptSeq` 있는 매매 | 60개월 실측에서 동당 반복거래 쌍 중앙값은 157개였고 341개 동 중 91개가 50쌍 미만이었다. hedonic은 모든 거래를 쓴다. 단순 중앙값은 그 분기에 어떤 단지가 거래됐는지에 따라 흔들린다 |
| 5a | 모형: `log(가격/㎡) = 단지×면적bin 고정효과 + 구×분기 효과 + 동×분기 편차`. 면적bin은 3㎡ 폭(임앤장 결정 11). **동×분기 편차에만 ridge 벌점 λ=5**를 걸어 표본이 적은 동을 구 지수 쪽으로 shrinkage한다 | 벌점은 분기마다 걸리므로 동의 편차는 n/(n+λ)만큼 남는다. 동×분기 거래 수 중앙값 9건이면 64%, 최소 표본 기준(결정 7, 연 20건 ≈ 분기 5건)인 동이면 50%가 남는다. λ=20이면 기준선 동이 20%만 남아 구 지수와 거의 같아진다. 결과를 보고 λ를 고르지 않는다 |
| 5b | 분기 단위 | 60개월 실측에서 동×분기 거래 수 중앙값은 9건, 19%는 0건, 모든 분기에 5건 이상인 동은 340개 중 100개였다. 월 단위는 불안정하다 |
| 5c | 반복거래 지수(같은 단지×면적bin의 연속 거래 쌍)는 **점검용으로만** 계산한다. 동별 연간 변화율에 대해 hedonic과의 Spearman 상관을 보고하고, 채택 판단에는 쓰지 않는다 | 구성 편향에 대한 독립 점검 |
| 6 | **target = 기점 분기 t부터 t+h까지 동 지수의 log 변화.** h = 2, 4, 8, 12분기(6·12·24·36개월) | 완결 분기만 쓴다. 데이터가 2026-08에 끝나므로 마지막 완결 분기는 2026Q2다 |
| 6a | target 지수는 전체 데이터로 한 번 추정한다. **feature에 쓰는 지수는 기점 t까지의 거래만으로 다시 추정한다** (vintage 지수) | 고정효과는 미래 거래의 영향을 받는다. 전체 데이터 지수로 모멘텀 feature를 만들면 미래 정보가 과거 값에 스며든다 |
| 7 | **최소 표본 기준: 기점 직전 4분기의 취소 제외 매매가 20건 미만인 동은 그 기점에서 학습·평가·서비스에서 모두 제외한다.** 화면에서는 "거래가 적어 예측하지 않음"으로 표시한다 | 60개월 실측으로 동당 연 매매 중앙값은 2022년 12건, 2025년 85건이다. 제외 동을 숨기지 않는 것은 임앤장 결정 42와 같은 원칙이다 |
| 8 | **평가 = rolling-origin OOT.** 기점 t의 모델은 s + h ≤ t인 기점 s의 표본만으로 학습한다 (h분기 gap). 평가 기점은 2016Q1부터 (2026Q2 − h)까지 | gap이 없으면 학습 target 기간이 평가 기점 이후와 겹친다. 전월세 feature가 2011년부터라, 2016Q1 이전은 학습 기점을 충분히 쌓기 위한 기간으로 둔다 |
| 9 | **baseline** — B1: 동의 직전 h분기 log 변화(vintage 지수)가 그대로 이어진다. B2: 서울 전체의 직전 h분기 log 변화(기점 t에 결정 7 기준을 통과한 동들의 변화를 동마다 같은 가중치로 평균)를 모든 동에 똑같이 적용한다. 참고로만 보고(채택 판단 제외): B0 변화 0, oracle 서울 미래 평균 | handoff에 적은 "서울 전체 평균 변화율"은 기점 시점에 알 수 없는 미래 값이라, 기점에 계산할 수 있는 형태로 정의했다. oracle은 "시장 전체 흐름을 미리 알아도 동별 차이를 얼마나 맞히는가"를 보는 참고값이다 |
| 10 | **채택 기준: 모든 평가 기점을 합친 동별 예측의 MAE가 B1과 B2 모두보다 낮고, (모델 − baseline) MAE 차이의 동 단위 block bootstrap(1,000회, seed 42) 95% CI 상한이 0보다 작을 것.** baseline 두 개 모두에 대해 성립해야 한다 | 겹치는 기점 때문에 CI가 좁게 나올 수 있다는 한계는 결과와 함께 적는다 |
| 11 | **예측 기간 결정 규칙:** 기준(결정 10)을 통과하면 h=4(12개월)를 기본으로 서비스한다. 4가 통과하지 못하면 통과한 기간 중 가장 짧은 기간을 쓴다. 모두 실패하면 예측 수치를 서비스하지 않고 사실정보만 보여 준다 | 팀 합의(2026-09-15). 결과를 본 뒤 조정할 가능성이 제기됐다. 조정하면 아래 deviation 규칙을 따른다 |
| 12 | **모델 = LightGBM**, 하이퍼파라미터는 임앤장 33 채택값으로 시작한다. 예측 구간은 시간×동 이중 분할 conformal로 만들고, "80% 보장"이라고 쓰지 않는다 | 임앤장 결정 55·59 |
| 13 | **feature는 기점 t까지 공개된 정보로만 만들고, 모든 feature 테이블에 `as_of_quarter`를 둔다.** 현재 상태 스냅샷(용적률, 접근성)은 시점 불변 가정을 한계로 공개한다. 기간 중 사용승인된 단지의 용적률은 사용승인 전 기점에 쓰지 않는다 | 임앤장 결정 48 |

| 14 | **feature 선별은 2016년 이전 기점(평가 기점 2014Q1~2015Q4, h=4)에서만 하고, 고른 feature 묶음을 고정한 뒤 공식 평가(결정 10)를 한 번 실행한다** (2026-09-15, 2006~ 데이터 결과 확인 전) | 공식 평가 기점(2016Q1~)에서 feature를 고르면 같은 데이터로 고르고 평가하는 선택 편향이 생긴다. 묶음 단위(가격 모멘텀·거래·전세·정비사업·입주·입지·금리규제)로 하나씩 뺀 MAE를 비교하고, 빼도 MAE가 나빠지지 않는 묶음은 제외한다. 60개월 데이터 점검 실행(2023Q2~ 기점)은 이 구간과 겹치지 않는다 |
| 15 | **서울 전역에 같은 값인 기준금리는 원값을 feature로 쓰지 않고, 동 전세가율과의 교호작용(`base_rate_pct × jeonse_ratio_4q`, `base_rate_change_4q × jeonse_ratio_4q`)으로만 넣는다.** 투기과열지구 여부는 2023Q1~2025Q3에 자치구마다 달라 원값을 유지한다 (2026-09-15) | handoff §6.3에 이미 적은 원칙인데 44 초판에서 빠졌다. **60개월 점검 실행(판정 아님)에서 발견했다**: 2025Q1~Q3 기점에서 실제 평균 +0.07인데 모델 평균 −0.11로, 금리 국면이 한 번뿐인 학습 구간에서 금리 수준을 시장 방향과 잘못 연결했다. 2006~ 데이터의 선별·공식 결과는 아직 보지 않았다 |

### 결정 14 선별 결과 (2026-09-15, 2014Q1~2015Q4 기점 8개, h=4, 1,932행)

| 제외한 묶음 | 모델 MAE | 전체 대비 차이 | 규칙 판정 |
|---|---:|---:|---|
| 없음 (전체) | 0.04572 | 0 | — |
| macro_regulation | 0.03944 | −0.00628 | **제외** |
| location | 0.04542 | −0.00030 | **제외** |
| redevelop | 0.04568 | −0.00004 | **제외** (차이가 잡음 수준이지만 규칙대로 적용) |
| price_momentum | 0.04593 | +0.00021 | 유지 |
| supply | 0.04651 | +0.00079 | 유지 |
| trade | 0.04824 | +0.00252 | 유지 |
| jeonse | 0.05104 | +0.00531 | 유지 |

- **고정한 공식 평가 설정: `--drop-groups macro_regulation,location,redevelop`**
- 묶음을 하나씩만 뺀 결과이며, 여러 묶음을 함께 뺀 조합은 따로 보지 않았다
- 이 구간에서 어떤 조합도 B2(0.03670)보다 낮지 않았다. 선별용 결과라 채택 판정(결정 10)에는 쓰지 않는다
- 정비사업·입지·규제 정보는 예측 feature에서 빠져도 화면의 사실정보로는 계속 보여 준다

## 공식 결과 (2026-09-15, 커밋 17c2f69로 선별 고정 후 1회 실행)

| # | 결정 | 근거 |
|---|---|---|
| 16 | **4개 기간(h=2·4·8·12) 모두 결정 10 기준을 통과했다. 결정 11에 따라 h=4(12개월)를 기본으로 서비스한다** | `output/44.2.oot_metrics.txt`. 아래 표. 개선 폭은 작다: h=4에서 B2 대비 MAE 약 10% 감소이고, 미래 서울 평균을 아는 oracle(0.0459)과의 차이가 크다. 기점 간 target 기간이 겹쳐 CI가 좁게 나올 수 있다 |
| 17 | **conformal 예측 구간(결정 12)은 목표 coverage 80%를 달성하지 못했다.** h=4 test coverage 64.1%, h=12는 38.1% | `output/45.1.oot_intervals.txt`. 연도별로 국면 전환기(h=4: 2017 31%, 2021 34%, 2022 34%)에 크게 떨어진다. 과거 잔차 분포가 유지된다는 가정이 시장 국면 전환에서 깨진다. 처리 방식은 결과 확인 후 결정이므로 deviation 규칙을 따른다 |

| h(분기) | 행 | 동 | 기점 | 모델 MAE | B1 | B2 | B0(참고) | oracle(참고) | 모델−B1 CI | 모델−B2 CI | 구간 coverage |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---|---|---:|
| 2 | 9,101 | 276 | 40 | 0.0571 | 0.0692 | 0.0587 | 0.0717 | 0.0340 | [−0.0135, −0.0109] | [−0.0023, −0.0010] | 72.5% |
| 4 | 8,610 | 273 | 38 | 0.0917 | 0.1095 | 0.1018 | 0.1262 | 0.0459 | [−0.0192, −0.0163] | [−0.0112, −0.0089] | 64.1% |
| 8 | 7,686 | 271 | 34 | 0.1593 | 0.2047 | 0.1963 | 0.2197 | 0.0644 | [−0.0490, −0.0419] | [−0.0395, −0.0343] | 63.9% |
| 12 | 6,889 | 270 | 30 | 0.2469 | 0.2631 | 0.2553 | 0.2710 | 0.0729 | [−0.0202, −0.0125] | [−0.0102, −0.0066] | 38.1% |

- 공통 한계: target 지수는 단지 FE를 20년간 고정해 노후화가 잔차로 넘어간다(잔차 SD 0.112). 정비사업 feature는 2026-06 파일 하나라 생존 편향이 있다(선별에서 제외됨)

## 사전 등록 절차 폐기

| # | 결정 | 근거 |
|---|---|---|
| 18 | **사전 등록 절차를 앞으로 쓰지 않는다** (2026-09-15, 팀 결정). 이후 모델·feature·구간 조정은 결과를 보면서 하고, 일반 결정으로 번호를 이어 기록한다 | 결정 16·17의 공식 결과는 선별 고정(커밋 17c2f69) 후 1회 실행한 값이라 그대로 유효하다. **이후 같은 평가 구간(2016Q1~)을 보며 조정한 결과는 "탐색적 결과"로 표시하고**, 발표에서 사전에 정한 기준으로 검증한 수치로 인용하지 않는다 |

## 모델 최적화 (2026-09-15~16)


프론트 결정(19~33)과 번호가 겹쳐 2026-09-16에 34~38로 옮겼다. 내용은 그대로다.
| # | 결정 | 근거 |
|---|---|---|
| 34 | **투기과열지구 feature(43)의 2006Q1~2011Q3 값을 원문대로 고친다.** 지수 시작 시점 서울 전역 지정 → 2008-11-07 강남3구만 유지 → 2011-12-22 전부 해제 → 2017-08-03 전역 지정 (2026-09-15) | 초판은 2017-08-03을 최초 지정으로 보고 그 전을 0으로 두었다. 이벤트 목록 원문 확인에서 국토부 정책Q&A(2008-11-07 해제, 서울 22개 구)와 국토해양부고시 제2011-795호(2011-12-22 강남3구 해제)를 찾아 직접 확인했다. 재실행 비교: 바뀐 행 311개가 모두 2006Q1~2011Q3이고, 모델 입력 구간(41 기점 2011Q4~)에서 바뀐 행은 0개, 기준금리 컬럼은 동일하다. 따라서 결정 14 선별과 결정 16·17 공식 결과는 영향이 없다 |
| 35 | **최적화 모델은 real-time target을 쓴다**: `y_rt[h][d,t]` = 기점 t+h까지의 거래로 추정한 vintage 지수의 t→t+h 변화(41의 `mom_{h}q`와 같은 계산). 기존 전체 데이터 지수 target(결정 6)은 민감도 기준으로만 보고한다 (2026-09-15) | sol 검토(F03): 전체 데이터 지수는 미래 거래로 과거 값이 수정돼 학습 label에 미래 정보가 들어간다. 공식 h=4 평가 8,610행에서 |차이| 평균 0.0098, 최대 0.2512. 해제 거래 시점은 복원할 수 없어 한계로 남는다 |
| 36 | **최적화 모델은 서울 시장 모델 + 동 상대 모델의 2단계 구조로 하고, 선택 규칙을 구현 전에 고정한다** (탐색적 선택 규칙, 사전 등록 아님). 시장: M0(서울 모멘텀) vs M1(3-feature ridge). 상대: R0(0) vs R1(ridge) vs R2(LightGBM), 기점별 재중심화 후 γ 축소. 튜닝 구간 2016Q1~2020Q4에서 기점 동일 가중 MAE 2% 이상 개선 시만 채택. 2021Q1~2025Q2는 점검 구간이며 합격 판정 없음. 상세는 `docs/model-optimization-plan.md` v3.1 | 공식 모델 오차 분해에서 이득은 시장 방향에서만 나오고 동 상대 예측은 0 예측보다 나빴다(결정 16 이후 분석). sol 방법론 검토 3회(F01~F14, N01~N12, K01~K09) 반영. 2016~2025는 이미 관찰된 기간이라 모든 결과는 탐색적이다 |
| 37 | **최적화 모델의 예측 구간은 경험적 구간으로 한다**: 최근 8개 성숙 기점의 순차 OOT 잔차를 기점 동일 가중으로 모은 80% 분위수. conformal 보장이 없으며 coverage는 선택 기준이 아니다 | 결정 17에서 기존 conformal 구간이 국면 전환기에 크게 미달했다. 겹치는 h=4 target과 시장 공통 오차 때문에 교환 가능성 가정이 성립하지 않는다(sol F12) |
| 38 | **해제 비율(`cancel_share_4q`)은 feature로 쓰지 않는다. 공식 모델(결정 16)이 이 feature를 포함한 것은 한계로 기록한다** | 해제 거래 표시가 2020년부터만 있어(2006~2019년 0) 해제 여부가 아니라 제도 시점을 나타낸다. 공식 모델의 trade 묶음에 포함돼 있었다 |
| 39 | **최적화 모델의 선택 결과는 시장 M0 + 상대 R0이며, 이를 그대로 채택한다. 결과적으로 예측은 서울 전체 모멘텀 baseline(B2)과 같고 동별 차이가 없다** (2026-09-16, 탐색적 결과) | 튜닝 구간(2016Q1~2020Q4, h=4)에서 시장 ridge M1은 baseline보다 8.4% 나빴고, 상대 모델 R1·R2의 개선은 0.1~0.3%로 2% 문턱에 못 미쳤다. 동 상대 신호는 h=2 recent 구간에서만 뚜렷하다(상관 0.29~0.34). 화면에서는 동별 비교 근거로 쓸 수 없고 서울 시장 전망으로 표기해야 한다. 수치는 `docs/model-performance.md` |
| 40 | **법정동 경계는 GIS Developer 2023-07판(원본 도로명주소 DB)을 쓰고, 출처를 화면에 표기한다** (2026-09-16) | 국가공간정보포털·공공데이터포털·서울 열린데이터광장 경로는 404·DNS 실패·제공 종료·변경금지 라이선스로 모두 막혔다(`docs/data-sources.md` §6). 서울 467개 법정동을 EPSG:4326 GeoJSON으로 변환했고 지수 동 346개 중 340개가 매칭된다. 나머지 6개는 원장 구 코드 불일치 동이다 |

## deviation 기록 (결정 18 이전까지만 사용)

사전 등록 규칙(결정 4~13)을 결과 확인 후 바꾸면 여기에 적었다. 한 줄마다 **원래 규칙 · 바꾼 규칙 · 바꾼 이유 · 결과를 본 뒤였는지**를 적는다. 원래 규칙의 결과와 바꾼 규칙의 결과를 발표 자료에 함께 싣는다.

| 날짜 | 대상 결정 | 원래 | 변경 | 이유 | 결과 확인 후 여부 |
|---|---|---|---|---|---|
| 2026-09-15 | 12 (예측 구간) | 시간×동 이중 분할 conformal, 목표 coverage 80% | 구간 계산 방식은 바꾸지 않는다. 대신 과거 검증 실측 coverage(h=4 64.1%)와 "시장 국면 전환기에는 더 낮음"을 화면·meta에 함께 공개한다 | 목표 80%에 못 미쳤다(결정 17). 결과를 보고 보정 방식을 바꾸면 과대적합 위험이 있어, 사용자와 합의해 방법은 유지하고 실측값을 공개하기로 했다 | 결과 확인 후 |

## 프론트 데이터 연결 (2026-09-16)

| # | 결정 | 근거 |
|---|---|---|
| 19 | **프론트는 Railway Postgres의 active snapshot을 먼저 읽고, 접속이 없거나 조회가 실패하면 저장소에 있는 샘플 JSON으로 폴백한다.** 폴백했다는 사실은 API 응답의 `source`와 화면 안내 문구로 드러낸다 | 실제 수치로 화면을 검증하는 것이 먼저다. 다만 DB 접근 권한이 없는 팀원이나 배포 환경에서도 화면이 비지 않아야 해서 폴백을 남겼다. 샘플을 진짜 데이터처럼 보이게 두면 오해를 만들기 때문에 출처를 화면에 적는다 |
| 20 | **동 목록과 메타는 서버에서 한 번 읽어 화면에 내려보내고, 동 상세는 선택 시점에 `/api/dong/{dong_id}`로 가져온다** | 동이 346개라 상세까지 모두 첫 응답에 실으면 초기 전송량이 커진다. 선택은 한 번에 하나뿐이라 필요한 시점에 가져오는 편이 단순하고, loading·error·재시도 상태도 자연스럽게 붙는다 |
| 21 | **법정동 경계 GeoJSON은 도입하지 않고, 2020년 이후 매매가 있는 단지 좌표의 평균을 동 대표 좌표로 삼아 마커 지도를 만든다** | 경계 데이터는 아직 출처가 정해지지 않았고, 확보해도 좌표계 정리와 매칭 작업이 더 필요하다. 지금 필요한 것은 "이 동이 어디인가"를 알려주는 것뿐이라 보유 데이터만으로 되는 대표 좌표를 쓴다. 346개 동 중 344개에 좌표가 잡힌다. 경계가 준비되면 색칠 지도로 바꾼다 |
| 22 | **`app.dong_prediction`이 비어 있는 동안에는 예측 상태를 `dong_index.eligible`로 파생한다.** eligible이면 `NOT_SERVED`, 아니면 `INSUFFICIENT_SALES`로 본다 | 값이 없는데 화면에 변화율을 만들어 내지 않기 위해서다. 지금은 모든 동이 두 상태 중 하나로 보이며, 예측이 적재되면 그 행의 `status`를 그대로 쓴다 |
| 23 | **동 상세의 규제 표시는 단지별 투기과열지구 대신 `regulation_summary`의 서울 아파트 토지거래허가구역 지정 여부를 쓴다** | 보유 데이터에 동 단위 투기과열지구 값이 없다. 빈 칸을 남기기보다 실제로 가진 값을 기준일과 함께 보여준다 |

단지를 동에 묶는 방법도 여기에 적어 둔다. `app.complex`에는 법정동 코드가 없어서 매매 기록의 `(sgg_cd, umd_nm)`으로 묶는다. 그래서 매매가 한 번도 없던 단지는 동 상세의 단지 목록에 나오지 않는다.

## 프론트 화면 레이아웃 (2026-09-16)

| # | 결정 | 근거 |
|---|---|---|
| 24 | **넓은 화면에서는 좌측 열을 화면 높이에 고정하고, 동 목록을 페이지로 나눠 번호로 넘긴다** | 동 346개를 한 번에 펼치니 문서 높이가 42,919px까지 늘었다. 그리드 항목은 기본이 `align-self: stretch`라 오른쪽 지도·상세 열도 같이 늘어나, 지도 아래로 4만 픽셀짜리 빈 공간이 생겼다. 좌측 열을 화면 높이로 잡고 검색·필터와 페이지 번호를 위아래에 고정하면 두 열이 같은 화면 안에 남는다. 한 열로 쌓이는 좁은 화면에서는 페이지 전체가 흐르도록 두고 페이지 크기만 적용한다 |
| 25 | **한 페이지 개수는 목록 칸 높이를 재서 정한다. 최소 8개, 최대 24개로 묶는다** | 높이에 정확히 맞추면 화면이 낮을 때 페이지당 3~4개가 되어 번호가 100개를 넘고, 번호로 넘기는 쪽이 스크롤보다 불편해진다. 최소 8개를 보장하면 낮은 화면에서는 목록 칸 안에서 조금 스크롤되지만 페이지 수가 44개 선에서 유지된다. 항목 높이는 태그 줄 수에 따라 달라서 평균으로 어림한다 |
| 26 | **예측 상태와 지역 태그 필터는 접어 두고, 몇 개가 걸렸는지만 요약으로 보여준다** | 칩 두 묶음이 좌측 열 919px 중 380px을 먹어 목록이 밀렸다. 접으면 250px로 줄어 목록 칸이 485px에서 621px로 늘어난다. 검색창과 자치구 선택은 계속 보이므로 자주 쓰는 경로는 그대로다. 펼침 상태는 브라우저의 `<details>`에 맡긴다. React에 `open` 값을 넘기면 다시 그릴 때마다 상태가 되돌아가 펼친 칩이 접힌다 |
| 27 | **넓은 화면에서 지도 높이는 좌측 동 목록 열을 기준으로 잡되, 아래로 `--map-peek`(7rem)만큼 줄인다.** 동 상세는 그 아래에 붙이고, 내용이 도착하면 아래에서 떠오르는 효과로 새로 생겼다는 것을 알린다 | 지도가 38dvh일 때는 오른쪽 열 아래가 비어 첫 화면이 허전했다. 좌측 열 높이에 맞추면 채워지지만 이번에는 지도로 꽉 차 보인다. 7rem을 남기면 상세 카드 윗부분이 비쳐 보여, 화면이 답답하지 않으면서 아래에 이어지는 내용이 있다는 것도 함께 알린다. 화면 밖에 생긴 것은 효과만으로는 보이지 않으므로 선택한 순간과 내용이 도착한 순간에 상세로 스크롤한다. 좌측 열은 sticky라 스크롤한 뒤에도 목록이 그대로 보인다. 효과를 줄이기로 한 사용자에게는 애니메이션과 부드러운 스크롤이 모두 꺼진다 |
| 28 | **화면 골격 치수는 `globals.css`의 CSS 변수 하나로 모으고, 머리말 높이는 실제로 재서 `--app-header-h`에 적는다** | 좌측 열 높이·sticky 위치·스크롤 여백이 저마다 49px과 81px을 코드에 박고 있었다. 머리말은 480px 미만에서 두 줄로 접혀 69px이 되므로 이 값들이 좁은 화면에서 어긋났고, 실제로 상세 제목이 머리말 뒤로 20px 들어갔다. `components/app-header.tsx`가 자기 높이를 재서 변수에 적고, `--app-column-h`와 `scroll-padding-top`이 그 값에서 파생된다 |
| 29 | **상세의 등장 효과는 `DongDetailPanel` 안에서 걸고, 스크롤 기준이 되는 `<section>`에는 걸지 않는다** | `scrollIntoView`는 transform이 반영된 위치를 기준으로 삼는다. 기준 요소에 등장 효과가 걸려 있으면 `translateY(20px)`만큼 더 내려간 자리에 멈추고, 효과가 끝나면 상세가 화면 위로 20px 올라가 제목이 잘린다. 효과를 패널 쪽으로 옮기면 스크롤 기준과 효과 대상이 서로 다른 파일에 있어 실수로 다시 합쳐질 일이 없다. 머리말을 피하는 여백도 요소마다 적지 않고 `scroll-padding-top` 한 줄로 처리한다 |
| 30 | **지도는 보이는 동을 모두 담는 범위로 맞춘다. 자치구를 고르면 그 자치구만 담긴 범위가 되어 지도가 확대된다** | 자치구를 골라도 지도가 서울 전체를 보여 주면 고른 자치구가 어디인지 눈으로 확인하기 어렵다. 범위 계산은 `lib/map.ts`의 `boundsOf`로 모아 Kakao 지도(`setBounds`)와 좌표 미리보기가 같은 값을 쓴다. 동이 하나만 남아도 최소 2.2km 범위를 유지해 지나치게 확대되지 않게 하고, 가장자리 동이 화면 끝에 붙지 않도록 15%를 여유로 둔다. 검색어나 태그로 목록이 줄어들 때도 같은 규칙이 적용된다. 동을 고를 때는 확대 수준을 건드리지 않고 `panTo`로 가운데만 옮긴다 |
| 31 | **화면에 보이는 문장은 `docs/front/UX-strategy.md`의 대화체 톤을 따른다.** `~합니다`, `~되었습니다` 대신 `~해요`, `~됐어요`를 쓰고, 오류 문구에는 다음에 할 일을 함께 적는다 | 문구마다 말투가 달라 서비스가 따로 노는 느낌을 줬다. 톤 문서가 이미 있으니 화면 문구 전체를 거기에 맞춘다. 데이터 용어(법정동, 자치구, 정비사업, 전세 비중, 용적률)와 태그 이름은 정확도가 먼저라 그대로 둔다. 바꾼 것은 문장의 말투와 행정적인 표현이다 |
| 32 | **예측 상태 라벨을 `예측 제공`·`표본 부족`·`예측 미제공`에서 `예측 있음`·`매매 적음`·`예측 없음`으로 바꾼다** | `미제공`은 행정 문서 표현이고 `표본`은 통계 용어라 결정 31의 "전문용어와 행정적 표현을 최소화한다"에 어긋난다. 상태가 뜻하는 바(예측이 있는지, 왜 없는지)는 그대로 전달된다. `PredictionStatus` enum 값(`PREDICTED`, `INSUFFICIENT_SALES`, `NOT_SERVED`)과 payload는 바꾸지 않았다 |
| 33 | **비교 카드 제목을 `서울·자치구·동 흐름`에서 `지난 N개월 실제 변화율`로 바꾸고, 비교 구간과 평균 방식을 카드 안에 적는다** | 바로 위 예측 카드가 `앞으로 12개월 추정 변화율`이고 비교 구간도 같은 12개월이라, 이 카드도 추정값으로 읽혔다. "흐름"이라는 말에는 과거인지 미래인지, 어느 기간인지가 담기지 않는다. 제목에 `지난`과 `실제`를 넣어 방향을 먼저 밝히고, 설명에 `2025년 2분기부터 2026년 2분기까지`처럼 구간을 적는다. 또 서울·자치구 값은 예측 대상 동의 동 단위 단순 평균이라 거래가 많은 동이 더 크게 반영되지 않는데(`lib/queries.ts`), 이 사실이 화면에 없어 평균을 오해하기 쉬웠다. 카드 하단에 한 줄로 적는다. 계산식은 바꾸지 않았다 |

## 프론트 법정동 경계 (2026-09-16)

| # | 결정 | 근거 |
|---|---|---|
| 41 | **법정동 경계는 동을 고르고 어디인지 알아보는 영역으로만 쓰고, 예측값으로 색을 칠하지 않는다.** 동 대표 좌표는 결정 21대로 단지 좌표 평균을 그대로 쓰고, 경계는 그 위에 덧그린다 | 결정 39에서 채택 모델이 M0+R0라 모든 동의 추정 변화율이 서울 전체 모멘텀과 같다. 동마다 색이 달라 보이는 choropleth는 없는 차이를 있는 것처럼 보여 준다. 경계를 넓은 클릭 영역과 위치 확인용으로만 쓰면 이 문제가 생기지 않는다. 나중에 과거 실적(매매 건수, 지난 변화율)으로 칠할 때는 결정 33처럼 제목에서 과거임을 먼저 밝힌다. 지도 범위(`boundsOf`)는 계속 대표 좌표로 잡는다. 경계 bbox로 바꾸면 가장자리 동의 빈 땅까지 화면에 들어와 결정 30의 확대 효과가 약해진다 |
| 42 | **경계는 DB가 아니라 `front/public/data/dong-boundary.geojson` 정적 파일로 내보내 쓴다.** `models/index/53.export_front_boundary.py`가 지수 동 340개만 남기고 좌표를 줄인다 | `app.dong_boundary`에 geometry가 들어 있지만 전체가 3.8MB라 요청마다 jsonb를 읽어 내려보낼 이유가 없다. 경계는 snapshot과 달리 거의 바뀌지 않아 정적 파일이 맞고, Next.js가 그대로 캐시한다. Douglas-Peucker 5.5m + 소수 5자리 반올림은 서울 전체가 보이는 확대 수준에서 화면 1픽셀보다 작다. 파일이 없으면 지도는 지금처럼 마커만 그리므로, 경계를 아직 만들지 않은 환경에서도 화면이 깨지지 않는다. 배포는 저장소 파일을 그대로 쓰므로 이 파일은 `output/`과 달리 commit한다 |
| 43 | **지도는 두 단계로 보여 준다. 넓게 보면 자치구 경계와 자치구 이름만 나오고, 자치구를 누르거나 확대하면 동 경계와 동 이름이 나온다.** 갈리는 기준은 Kakao 확대 수준 7이다 | 동 340개의 경계와 이름을 서울 전체 화면에 한꺼번에 그리면 이름이 겹쳐 아무것도 읽히지 않는다. 처음 보는 사람이 먼저 고르는 단위도 자치구다. 자치구를 누르면 자치구 필터가 걸려 목록과 지도가 함께 좁혀지므로, 지도와 왼쪽 목록이 따로 놀지 않는다. 확대 수준으로 단계를 나누면 사용자가 손으로 확대해도 같은 규칙이 적용된다 |
| 44 | **자치구 경계는 따로 받지 않고 동 경계에서 만든다.** 자치구 안쪽 선(두 동이 공유하는 선분)을 지우고 남은 선을 이어 붙인다 | 경계 원천을 하나로 유지해야 자치구 선과 동 선이 정확히 겹친다. 서로 다른 출처를 쓰면 확대했을 때 두 선이 어긋나 보인다. 공유 선분 판정은 좌표가 정확히 같아야 하므로 점을 줄이기 전에 먼저 한다. 지수에 없는 동까지 모두 넣어야 자치구 안에 구멍이 생기지 않는다. 자치구마다 색을 다르게 주지는 않는다. 색이 다르면 값이 다르다는 뜻으로 읽히는데(결정 39) 실제로는 구역을 나누는 선일 뿐이다 |
| 45 | **동 경계와 자치구 경계를 모두 법정동 원천(52.1)에서 만든다. 52.1은 SHP를 다시 돌리지 않고 `app.dong_boundary`에서 내려받는다** (2026-09-16) | 지수가 법정동 기준이라 경계도 법정동이어야 한다. 받은 행정동 파일(425개)은 지수 법정동 346개 중 169개(49%)만 이름이 맞고, 청운효자동처럼 법정동 여러 개를 묶은 행정동은 다시 쪼갤 수 없어 동 경계로 쓸 수 없었다. 그런데 `app.dong_boundary`에 52의 결과가 그대로 적재돼 있어(467행, `in_index` 340행, source 컬럼도 GIS Developer) 원천 SHP 없이 52.1을 복원할 수 있다. 두 레이어가 같은 원천에서 나오므로 자치구 선과 동 선이 정확히 겹치고, 출처 표기도 결정 40 문구 하나로 끝난다 |
| 46 | **자치구 경계를 합칠 때 원천의 작은 틈은 2m까지 이어 붙이고, 980m² 미만 조각은 버린다** | 맞닿은 두 법정동이 같은 경계선을 조금씩 다르게 그려 두 가지 문제가 생겼다. 성북구에서는 두 점이 0.11m 어긋나 자치구 테두리가 한 군데 끊겼고, 자치구마다 1~31m²짜리 가느다란 조각이 수십 개씩 남았다. 틈은 이어 붙이고 조각은 버린다. 진짜 자치구 조각은 1,600만m² 이상이라 기준값 사이 간격이 충분히 넓다. 한도를 넘게 벌어지면 원천이 잘못된 것이므로 이어 붙이지 않고 멈춘다 |
| 47 | **브랜드색을 `#5520A8`로 바꾸고 파생색을 여기에 맞춘다.** `--primary-hover`는 22% 어둡게(`#421983`), `--primary-soft`는 흰색에 8%만 섞어(`#f1edf8`) 만든다 | 색은 한 곳(`globals.css`)에서만 정하고 나머지는 그 값에서 파생시킨다. 지도 도형 색도 CSS 토큰을 읽어 쓰므로 함께 바뀐다. 대비는 흰 글씨 9.7:1, 흰 배경 9.7:1, soft 배경 위 8.4:1로 WCAG AAA 기준을 모두 넘는다 |
| 48 | **자치구를 고르면 지도 범위를 그 자치구 도형에 맞춘다** | 동 대표 좌표만으로 범위를 잡으면 단지가 몰려 있는 쪽으로 치우쳐 자치구가 화면 한쪽에 몰린다. 자치구 경계 도형 전체를 담으면 자치구가 가운데에 온다. 경계 파일이 없으면 지금까지처럼 대표 좌표로 잡는다(결정 30) |

